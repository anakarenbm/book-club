<!DOCTYPE html>
<html>
  <head>
    <title>BookClub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">BookClubsApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="#">My Profile</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="#">My Clubs</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="#">Join a Club</a>
          </li>
        </ul>
        <span class="nav-item dropdown navbar-nav ml-auto">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if user_signed_in? %>
              <%= current_user.email %>
            <% else %>
              Welcome!
            <% end %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
            <%= link_to('Log Out', destroy_user_session_path, method: :delete, class: "dropdown-item") %>
            <% else %>
            <%= link_to('Log In', new_user_session_path, class: "dropdown-item")  %>
            <%= link_to('Sign Up', new_user_registration_path, class: "dropdown-item")  %>
            <% end %>
          </div>
        </span>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="container">
      <%= yield %>
    </div>
    <footer>
      <div class="container">
        <span class="text-muted">Online Book Clubs <%= Time.now.year %></span>
      </div>
    </footer>
  </body>
</html>
