<div class="dropdown float-right">
  <button type="button" class="btn btn-icon btn-large btn-ghost ml-2 border" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <%= icon_tag "menu" %>
  </button>

  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="admin-actions">
    <%= link_to 'Edit Club', edit_club_path(@club), class: "dropdown-item" %>
    <%= link_to 'Destroy', @club, method: :delete, data: { confirm: 'Are you sure?' }, class: "dropdown-item" %>
  </div>
</div>
